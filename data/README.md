# Data

This directory is for local state only.

- `competitor_intel.db` is the SQLite database created during development.
- `runtime/` is a good place for local logs or scratch outputs if you need them.

The app should run fine if this folder starts nearly empty.
